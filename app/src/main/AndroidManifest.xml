<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="jmapps.arabicinyourhands">

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainChaptersActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChaptersLessonsActivity"
            android:label="@string/title_activity_chapter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ContentsLessonsActivity" />
        <activity android:name=".ExercisesActivity"
            android:label="@string/action_exercises"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>
</manifest>